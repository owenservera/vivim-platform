"use client"

import { motion } from "framer-motion"
import {
  Package,
  Layers,
  Network,
  Cpu,
  Shield,
  Code2,
  ArrowRight,
  Users,
  Database,
  Lock,
  Key,
  Brain,
  MessageSquare,
  FileCode
} from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Navigation } from "@/components/navigation"
import { Footer } from "@/components/footer"

const sdkFeatures = [
  { icon: Layers, title: "6-Layer Architecture", description: "Complete protocol stack.", category: "Architecture" },
  { icon: Network, title: "P2P Networking", description: "Peer-to-peer connectivity.", category: "Network" },
  { icon: Cpu, title: "AI Integration", description: "AI worker support.", category: "AI" },
  { icon: Shield, title: "Chain of Trust", description: "Identity and trust.", category: "Security" },
  { icon: Database, title: "CRDT Storage", description: "CRDT data types.", category: "Storage" },
  { icon: Code2, title: "CLI Tools", description: "Command-line.", category: "DevEx" },
]

const builtInNodes = [
  { name: "Identity", description: "DID management", icon: Key },
  { name: "Storage", description: "Content storage", icon: Database },
  { name: "Content", description: "Content retrieval", icon: FileCode },
  { name: "Social", description: "Social graph", icon: Users },
  { name: "AI Chat", description: "AI interfaces", icon: MessageSquare },
  { name: "Memory", description: "Knowledge graph", icon: Brain },
]

const builtInApps = [
  { name: "ACU Processor", status: "stable" },
  { name: "OmniFeed", status: "stable" },
  { name: "Circle Engine", status: "stable" },
  { name: "AI Documentation", status: "stable" },
  { name: "Crypto Engine", status: "stable" },
  { name: "Assistant Engine", status: "stable" },
  { name: "Tool Engine", status: "stable" },
  { name: "AI Git", status: "stable" },
  { name: "Roadmap Engine", status: "stable" },
  { name: "Publishing Agent", status: "stable" },
  { name: "Public Dashboard", status: "stable" },
]

const bundleFeatures = [
  { bundle: "Identity & Auth", progress: 90 },
  { bundle: "Conversation & Messaging", progress: 85 },
  { bundle: "Social Graph", progress: 75 },
  { bundle: "Feed & Discovery", progress: 70 },
  { bundle: "AI & Assistant", progress: 65 },
  { bundle: "Storage & Sync", progress: 80 },
  { bundle: "Network & Infrastructure", progress: 85 },
  { bundle: "Chain & Trust", progress: 60 },
]

export default function SDKPage() {
  return (
    <div className="min-h-screen flex flex-col">
      <Navigation />
      <main className="flex-1">
        <section className="relative overflow-hidden py-20 sm:py-32">
          <div className="absolute inset-0 bg-gradient-to-br from-primary/10 via-background to-background" />
          <div className="container relative">
            <motion.div initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }} className="text-center max-w-4xl mx-auto">
              <Badge variant="secondary" className="mb-4"><Package className="w-3 h-3 mr-1" />v1.0.0 Released</Badge>
              <h1 className="text-4xl sm:text-6xl font-bold tracking-tight">VIVIM <span className="text-primary">SDK</span></h1>
              <p className="mt-6 text-xl text-muted-foreground max-w-2xl mx-auto">The <strong>Bun-native</strong> toolkit for building decentralized, local-first applications with AI capabilities.</p>
              <div className="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
                <Button size="lg" asChild><a href="#quick-start">Get Started<ArrowRight className="ml-2 h-4 w-4" /></a></Button>
                <Button size="lg" variant="outline" asChild><a href="https://github.com/owenservera/vivim-app/tree/main/sdk" target="_blank"><Code2 className="mr-2 h-4 w-4" />View Source</a></Button>
              </div>
              <div className="mt-12 grid grid-cols-2 sm:grid-cols-4 gap-4 max-w-2xl mx-auto">
                {[{ label: "Built-in Nodes", value: "6" }, { label: "Apps", value: "11" }, { label: "Bundle Features", value: "80+" }, { label: "Lines of Code", value: "25K+" }].map((stat) => (
                  <motion.div key={stat.label} whileHover={{ scale: 1.05 }} className="p-4 rounded-lg bg-card/50 border">
                    <div className="text-2xl font-bold">{stat.value}</div>
                    <div className="text-sm text-muted-foreground">{stat.label}</div>
                  </motion.div>
                ))}
              </div>
            </motion.div>
          </div>
        </section>

        <section className="py-20 bg-muted/50">
          <div className="container">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12">
              <h2 className="text-3xl font-bold tracking-tight">Everything You Need</h2>
              <p className="mt-4 text-muted-foreground max-w-2xl mx-auto">A complete toolkit for building decentralized applications.</p>
            </motion.div>
            <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
              {sdkFeatures.map((feature, index) => (
                <motion.div key={feature.title} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }}>
                  <Card className="h-full">
                    <CardHeader>
                      <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4"><feature.icon className="w-6 h-6 text-primary" /></div>
                      <CardTitle>{feature.title}</CardTitle>
                      <Badge variant="secondary" className="w-fit">{feature.category}</Badge>
                    </CardHeader>
                    <CardContent><p className="text-muted-foreground">{feature.description}</p></CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section className="py-20">
          <div className="container">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12">
              <h2 className="text-3xl font-bold tracking-tight">6 Built-in Nodes</h2>
              <p className="mt-4 text-muted-foreground max-w-2xl mx-auto">Core infrastructure for your decentralized app.</p>
            </motion.div>
            <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
              {builtInNodes.map((node, index) => (
                <motion.div key={node.name} initial={{ opacity: 0, scale: 0.95 }} whileInView={{ opacity: 1, scale: 1 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }} className="flex items-center gap-4 p-4 rounded-lg border bg-card">
                  <div className="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center"><node.icon className="w-5 h-5 text-primary" /></div>
                  <div><div className="font-medium">{node.name}</div><div className="text-sm text-muted-foreground">{node.description}</div></div>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section className="py-20 bg-muted/50">
          <div className="container">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12">
              <h2 className="text-3xl font-bold tracking-tight">11 Built-in Apps</h2>
              <p className="mt-4 text-muted-foreground max-w-2xl mx-auto">Pre-built applications for decentralized features.</p>
>
            <div className="grid sm:grid-col            </motion.divs-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
              {builtInApps.map((app, index) => (
                <motion.div key={app.name} initial={{ opacity: 0, y: 10 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.05 }} className="flex items-center justify-between p-4 rounded-lg border bg-card">
                  <span className="font-medium">{app.name}</span>
                  <Badge variant="default">{app.status}</Badge>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section className="py-20">
          <div className="container">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12">
              <h2 className="text-3xl font-bold tracking-tight">Feature Bundles</h2>
              <p className="mt-4 text-muted-foreground max-w-2xl mx-auto">Comprehensive feature sets organized into bundles.</p>
            </motion.div>
            <div className="max-w-3xl mx-auto space-y-6">
              {bundleFeatures.map((bundle, index) => (
                <motion.div key={bundle.bundle} initial={{ opacity: 0, x: -20 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.1 }} className="space-y-2">
                  <div className="flex items-center justify-between">
                    <span className="font-medium">{bundle.bundle}</span>
                    <span className="text-sm font-medium">{bundle.progress}%</span>
                  </div>
                  <div className="h-2 rounded-full bg-muted overflow-hidden">
                    <motion.div initial={{ width: 0 }} whileInView={{ width: bundle.progress + "%" }} viewport={{ once: true }} transition={{ duration: 0.8, delay: index * 0.1 }} className="h-full bg-primary rounded-full" />
                  </div>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        <section id="quick-start" className="py-20 bg-muted/50">
          <div className="container">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12">
              <h2 className="text-3xl font-bold tracking-tight">Quick Start</h2>
              <p className="mt-4 text-muted-foreground max-w-2xl mx-auto">Get started with VIVIM SDK.</p>
            </motion.div>
            <div className="max-w-2xl mx-auto rounded-lg border bg-card p-6">
              <pre className="overflow-x-auto text-sm text-muted-foreground">bun install @vivim/sdk</pre>
              <pre className="mt-4 overflow-x-auto text-sm text-muted-foreground">import {"{ VivimSDK }"} from '@vivim/sdk'</pre>
            </div>
          </div>
        </section>
      </main>
      <Footer />
    </div>
  )
}
