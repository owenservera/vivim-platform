# Z.ai Link - SingleFile CLI Settings
# URL: https://chat.z.ai/s/e6b0a2c9-8439-4869-94aa-e46bed7e8040

node single-file-node.js "URL" "OUTPUT" \
  --browser-wait-until=networkIdle \
  --browser-wait-delay=5000 \
  --browser-wait-until-delay=2000 \
  --browser-load-max-time=120000 \
  --browser-capture-max-time=120000 \
  --browser-width=1920 \
  --browser-height=1080 \
  --block-scripts=false \
  --load-deferred-images=true \
  --load-deferred-images-max-idle-time=3000 \
  --remove-hidden-elements=false \
  --compress-HTML=false

# SETTINGS EXPLANATION:
# --browser-wait-until=networkIdle      : Wait until network is idle (no more network activity)
# --browser-wait-delay=5000             : Wait 5 seconds before capturing page
# --browser-wait-until-delay=2000       : Wait 2 seconds after network idle condition
# --browser-load-max-time=120000        : Max 2 minutes for page load
# --browser-capture-max-time=120000     : Max 2 minutes for page capture
# --browser-width=1920 --browser-height=1080 : Full HD viewport
# --block-scripts=false                 : DON'T block scripts (allow JS to run)
# --load-deferred-images=true           : Load lazy-loaded images
# --load-deferred-images-max-idle-time=3000 : Wait 3s for deferred images
# --remove-hidden-elements=false        : Keep hidden elements (might contain content)
# --compress-HTML=false                 : Don't compress HTML (keep readable)

# RESULT:
# Output: html-dls-cli/zai-cli.html
# Size: ~1.6 MB
# Lines: 366
